name: Publish course

# On every push to main: restore the artifact cache, render the affected course
# modules for real, regenerate the debug dashboard, and publish it to the
# gh-pages site root. Manual dispatch still renders every module.

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: course-publish
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up build environment
        uses: ./.github/actions/setup-build
        timeout-minutes: 45

      - name: Restore artifact cache from gh-pages
        working-directory: webinar-builder
        run: npm run ci:restore

      - name: Detect affected projects
        if: github.event_name == 'push'
        id: affected
        working-directory: webinar-builder
        env:
          BASE_REF: ${{ github.event.before }}
        run: npm run -s ci:affected

      - name: Render + stitch affected modules (PAID — cache-gated)
        if: github.event_name == 'push' && steps.affected.outputs.any == 'true'
        working-directory: webinar-builder
        env:
          NO_SCREENCAST: "1"
          VERTEX_API_KEY: ${{ secrets.VERTEX_API_KEY }}
          WAVESPEED_API_KEY: ${{ secrets.WAVESPEED_API_KEY }}
          HEYGEN_API_KEY: ${{ secrets.HEYGEN_API_KEY }}
          BYTEPLUS_ACCESS_KEY_ID: ${{ secrets.BYTEPLUS_ACCESS_KEY_ID }}
          BYTEPLUS_SECRET_ACCESS_KEY: ${{ secrets.BYTEPLUS_SECRET_ACCESS_KEY }}
          REPLICATE_API_KEY: ${{ secrets.REPLICATE_API_KEY }}
          ASTRIA_AUTH_TOKEN: ${{ secrets.ASTRIA_AUTH_TOKEN }}
          GEMINI_TUNE_ID: ${{ secrets.GEMINI_TUNE_ID }}
          WORKSPACE_ID: ${{ secrets.WORKSPACE_ID }}
          ASTRIA_BASE_URL: ${{ secrets.ASTRIA_BASE_URL }}
        run: |
          export HF_WORKERS=$(nproc)
          echo "rendering: HF_WORKERS=$HF_WORKERS, one segment at a time"
          for p in $(echo '${{ steps.affected.outputs.projects }}' | tr -d '[]"' | tr ',' ' '); do
            echo "::group::render $p"
            npx tsx pipeline/build.ts --project "$p" --all --parallel 1
            npx tsx pipeline/stitch.ts --project "$p" || echo "stitch skipped for $p"
            echo "::endgroup::"
          done

      - name: Render + stitch every module (manual PAID — cache-gated)
        if: github.event_name == 'workflow_dispatch'
        working-directory: webinar-builder
        env:
          NO_SCREENCAST: "1"
          VERTEX_API_KEY: ${{ secrets.VERTEX_API_KEY }}
          WAVESPEED_API_KEY: ${{ secrets.WAVESPEED_API_KEY }}
          HEYGEN_API_KEY: ${{ secrets.HEYGEN_API_KEY }}
          BYTEPLUS_ACCESS_KEY_ID: ${{ secrets.BYTEPLUS_ACCESS_KEY_ID }}
          BYTEPLUS_SECRET_ACCESS_KEY: ${{ secrets.BYTEPLUS_SECRET_ACCESS_KEY }}
          REPLICATE_API_KEY: ${{ secrets.REPLICATE_API_KEY }}
          ASTRIA_AUTH_TOKEN: ${{ secrets.ASTRIA_AUTH_TOKEN }}
          GEMINI_TUNE_ID: ${{ secrets.GEMINI_TUNE_ID }}
          WORKSPACE_ID: ${{ secrets.WORKSPACE_ID }}
          ASTRIA_BASE_URL: ${{ secrets.ASTRIA_BASE_URL }}
        run: |
          export HF_WORKERS=$(nproc)
          echo "rendering: HF_WORKERS=$HF_WORKERS, one segment at a time"
          for f in script/projects/*.yaml; do
            p=$(basename "$f" .yaml)
            echo "::group::render $p"
            npx tsx pipeline/build.ts --project "$p" --all --parallel 1
            npx tsx pipeline/stitch.ts --project "$p" || echo "stitch skipped for $p"
            echo "::endgroup::"
          done

      - name: Lint compositions
        working-directory: webinar-builder
        run: npx hyperframes@0.4.9 lint --json > lint.json || true

      - name: Build debug dashboard
        if: ${{ success() }}
        working-directory: webinar-builder
        env:
          BUILD_MODE: main
          GIT_SHA: ${{ github.sha }}
          PAGES_BASE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
        run: npm run dashboard

      - name: Publish to gh-pages site root + save cache
        if: ${{ success() }}
        working-directory: webinar-builder
        run: npm run ci:publish -- root

      - name: Assemble Pages artifact
        if: ${{ success() }}
        working-directory: webinar-builder
        run: |
          rm -rf _site
          mkdir -p _site
          cp -a site/. _site/
          cp -a out _site/videos
          touch _site/.nojekyll

      - name: Upload Pages artifact
        if: ${{ success() }}
        uses: actions/upload-pages-artifact@v3
        with:
          path: webinar-builder/_site

      - name: Deploy to GitHub Pages
        if: ${{ success() }}
        id: deployment
        uses: actions/deploy-pages@v4
